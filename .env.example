# Directus (headless CMS) connection settings
# Directus lets non-technical users update website content without code changes.
# DIRECTUS_TOKEN is a secret: keep it server-side only and never expose via NEXT_PUBLIC_*
# Use a least-privileged, read-only token scoped to only required collections.
DIRECTUS_URL=
DIRECTUS_TOKEN=
